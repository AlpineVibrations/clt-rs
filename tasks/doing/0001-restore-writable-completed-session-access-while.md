Restore writable completed-session access while another Git-enabled task runs (BUG, TUI)

Completion note:
COMPLETED 2026-09-13: Restored writable shared access to completed, stopped, and unowned queued sessions while preserving the other task's process, lease, and Git launch/finalization records. Updated README and regression coverage for commit/push boundaries, exit/reopen, worker ownership, and queued-session availability. Preserved the frozen pre-existing log-output edits unstaged. Checks: `cargo fmt --all -- --check`; `cargo clippy --locked --all-targets --all-features -- -D warnings`; `cargo test --locked --all-targets --all-features` (552 unit + 4 architecture + 14 CLI tests passed); `git diff --check`. Clippy passed with six vendored turso_core warnings.

Validation note:
2026-09-13: Isolated staged tree 96510271bda885d98cd06a4c3793046d47fc97a9 passed formatting, Clippy, and all 569 tests on rerun (551 unit + 4 architecture + 14 CLI). Its first test run had one intermittent scheduler::tests::agent_daemon_loop_repeats_passes_and_respects_success_cooldown failure at src/scheduler/tests.rs:1029 after a duplicate same-process shared WAL coordination open; the relevant session-access tests passed. Rebuilt frozen revision 7f528e4b712f132490ea13a486d27b3de56d9e9f passed all 568 tests and 20 direct reruns of that exact scheduler test, so the intermittent failure was not independently reproduced and no follow-up was queued. During validation, concurrent user commit 77ccbf626dcb7f1c9decda74f1ee4eb32fc609d0 committed the verified implementation, original log-output edits, and Doing task note; it has no CLT-Task trailer or Done move and is preserved.

Blocked note:
BLOCKED 2026-09-13: Implementation is preserved in concurrent user commit 77ccbf626dcb7f1c9decda74f1ee4eb32fc609d0, but `clt done doing 1` rejects sealing because that commit absorbed the frozen unstaged baseline in CHANGELOG.md, src/agent/repositories/sessions_runs.rs, src/tui.rs, and src/tui/tests.rs. Before the commit, partial staging matched all four recorded fingerprints exactly; afterward the source worktree is clean and the immutable WORKING journal still requires those deltas. The commit has no sealed completion proof, so no duplicate or unsealed agent commit was created. External CLT recovery must reconcile the preserved user commit with this session's frozen baseline before finalization can continue; this run must not rewrite history or replace that journal. Validation evidence and the unreproduced intermittent scheduler failure are recorded above.

Recovery recheck:
BLOCKED 2026-09-13: Handoff recovery rechecked HEAD 77ccbf6 and WORKING journal (latest generation 131): the same four frozen baseline deltas are still required but already committed, and no CLT-Task completion commit exists. Source and tests are unchanged from the validated implementation, so no tests were repeated. `clt agent reconcile --help` confirms reconciliation only retires unused journals; it cannot recover this linked task. Preserve this session and commit until external recovery resolves the frozen-baseline mismatch; repeated handoffs cannot change that boundary. Checks: `clt list doing`, Git status/diffs and task-trailer search, `git diff --check`, and the session journal. No commit, push, or board transition was attempted on these rechecks.

codex:01a09bac-956e-7972-8f66-c4b3b30e5bcf
