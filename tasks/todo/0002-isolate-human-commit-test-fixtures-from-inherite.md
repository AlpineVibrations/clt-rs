Isolate human-commit test fixtures from inherited agent Git identity — Follow-up evidence: On this task, cargo test --locked --all-targets --all-features passed 521/523 library tests but failed managed_git::tests::advanced_branch::task_finalization_accepts_concurrent_user_commits_and_board_checkpoints at advanced_branch.rs:192 (repair_working_git_task_link assertion) and managed_git::tests::premanifest_audit_accepts_a_user_commit_without_a_task_journal at tests.rs:3396 (agent_git_range_is_safe_before_manifest assertion). Both reproduce unchanged on frozen starting revision dc9c8df062a946d75a7eda806cd7c4cfd9c01bde exported with git archive into an isolated directory: CARGO_TARGET_DIR=/Users/pro/code/lls/clt/target cargo test --locked --lib --all-features user_commit -- --test-threads=1 gives 0 passed/2 failed at matching assertions. Both pass there (2/2) when prefixed with env -u GIT_AUTHOR_NAME -u GIT_AUTHOR_EMAIL -u GIT_COMMITTER_NAME -u GIT_COMMITTER_EMAIL. The automated environment supplies CLT Agent <clt-agent@localhost>, overriding fixture repository configuration. Remaining work: isolate simulated human Git commits from inherited author/committer variables while preserving explicit agent-identity test coverage. All TUI tests, formatting, and Clippy passed; this is independent of the Kanban console title change. clt-follow-up:01a08c08-7ae0-79d0-bc01-ccec2c901fb3
