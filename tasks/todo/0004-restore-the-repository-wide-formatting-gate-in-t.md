Restore the repository-wide formatting gate in the existing orphan-journal code — Follow-up evidence: Frozen launch revision 7e5d2782dba669d319f6e98b2f224ee98e777ee2 was extracted with git archive into an isolated temporary directory. Baseline and current checkout both fail rustfmt --edition 2024 --check build.rs src/main.rs src/lib.rs tests/architecture.rs tests/cli.rs with identical normalized output: src/agent.rs:175, src/managed_git/tests/orphan.rs:613, src/scheduler.rs:32, and src/worker.rs:1227. The Shift-input task passes its 112 TUI tests, all 655 repository tests (one optional test ignored), Clippy, and changed-file formatting. Remaining work: format those four pre-existing regions and rerun the full formatting gate; overlap is already tracked in Todo 0004, so reconcile that existing item when this follow-up runs. clt-follow-up:01a0bf6d-5677-7e50-8f3f-7e74d3c689b7
