Fix stale idle agent registry state and recovery after interrupted updates (BUG, HIGH)

Completion note:
COMPLETED 2026-09-14: Fixed Turso reusing a local disk scan after adopting another process's newer WAL metadata. Connection-open reconciliation now consumes each scan once under a local mutex, preventing stale index publication, resurrected leases, and stale page writes. Recognized index-page panics retain the storage cause and fence later database operations. Added a multiprocess regression covering partial-checkpoint reopen, idle peer writes, lease replacement, project disable, reopen, and integrity; it failed before the fix and passes afterward. Verified 557 unit tests with four test threads, 4 architecture tests, 14 CLI integration tests, formatting, strict Clippy, and diff checks. An initial unconstrained run exposed an existing concurrent-open race in a scheduler test; that test passed in the complete rerun. Changes are source-only; the existing damaged live registry was preserved and still requires recovery.
Release note: Prepared patch 0.6.14 with the idle-registry fix and storage-error reporting changes for the requested commit and local installation.
