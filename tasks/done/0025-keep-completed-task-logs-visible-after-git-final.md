Keep completed-task logs visible after Git finalization recovery (BUG, TUI)

Completion note:

COMPLETED 2026-09-13: Task and project log views now find the latest output-bearing run for the exact project/session, skipping newer Git-recovery acknowledgements without log paths. Preserved outcome history, run settings, completed-versus-live detection, and separation between task sessions. Reproduced a registered project's missing-log failure before the fix and verified its existing session resolves to its original run afterward. Added recovery and unrelated-session regressions. Checks: cargo fmt --all -- --check; cargo clippy --locked --all-targets --all-features -- -D warnings; 98 TUI tests; cargo test --locked --all-targets --all-features -- --test-threads=8 (551 unit, 4 architecture, 14 CLI tests passed); release build and local installation; git diff --check. The first fully parallel suite hit an unrelated database-health fixture lease-setup failure; its isolated rerun and the full rerun passed. Confirmed the separately reported shared-Codex restriction belongs to another still-running task in that project; did not interrupt that task or alter its Git ownership.
