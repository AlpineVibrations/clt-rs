Remove the default agent run deadline so tasks can finish without an elapsed-time cutoff (BUG, AGENT)

Completion note:
COMPLETED 2026-09-08: Disabled the default run deadline and made CLT_AGENT_RUN_TIMEOUT_SECONDS=0 explicitly unlimited. Both supervisor and direct-child wait loops continue without an elapsed-time cutoff while heartbeats, renewable leases, explicit stop, and opt-in positive deadlines remain active. Heartbeat logs label unlimited runs clearly; updated README and changelog. Tests cover unset/zero/positive/invalid settings, unlimited completion and lease renewal, explicit stop, and opt-in deadline enforcement. Corrected a pre-existing supervisor test cleanup race by waiting for its registry writes to finish before deleting the fixture. Checks: cargo fmt --all -- --check; cargo clippy --locked --all-targets --all-features -- -D warnings; cargo test --locked --all-targets --all-features -- --test-threads=4 (510 passed); cargo build --release --locked. Installed the verified binary and reloaded only the scheduler; its snapshot hash matches and no deadline override is configured. FISH-26 completed naturally during this work. FISH-27 remains running in its previous worker generation, retaining that generation's old deadline until the run completes or the user chooses a same-session restart.
