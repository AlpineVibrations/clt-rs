Prevent orphaned resume sessions from monopolizing projects and recover Meshdock's stranded scheduler state (BUG, AGENT, HIGH) — COMPLETED 2026-09-01: The scheduler now retires markerless `resume_requested` sessions only when no worker or live lease can own them, worker renewal failures include exact durable-fence diagnostics, and `clt agent retry [PATH]` safely clears one inactive project's failure cooldown. Recovered the corrupted live Turso database into an integrity-checked replacement while preserving registrations, settings, and recoverable history; deployed the patched release and confirmed Meshdock moved its next task to Doing with a running worker whose lease renewed past the prior 15-second failure boundary. Checks: `cargo fmt --check`; `cargo test` (322 passed); `cargo build --release`; recovered database `PRAGMA integrity_check` (`ok`); installed-binary hash comparison; live `clt agent status` and Meshdock board verification.
