Fix recurring agent registry shared WAL publication failure and recovery diagnostics (BUG, HIGH)

Completion note:
COMPLETED 2026-09-11: Fenced Turso coordination reseeding with checkpoint and writer locks, refreshed its snapshot under those locks, and preserved lock ownership through reader repair. Added native/process-scoped exclusion and unwind coverage plus a multiprocess regression with partial checkpointing, connection churn and 2,000 writes. The TUI now includes the underlying recovery error. Recovered the live registry by rebuilding coordination files with an integrity check, preserving the original DB/WAL bundle without snapshot reconstruction, and installed the release binary. Checks: cargo test --locked --all-targets --all-features -- --test-threads=4 (527 unit, 4 architecture and 14 CLI tests passed); vendored core storage tests (381 passed, 9 ignored); Clippy with warnings denied; formatting and diff checks.

Validation note: Two default-parallel full-suite runs each passed 526 unit tests and failed one different process-lifecycle test: agent_daemon_loop_repeats_passes_and_respects_success_cooldown observed a duplicate same-process Turso open during teardown; reaped_guardian_exits_for_registry_recovery_and_releases_database_access observed a transient access lock after recovery. Both passed in the complete four-thread run. These intermittent parallel failures remain an investigation item.
