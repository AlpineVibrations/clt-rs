ok new feature when a task is in fresh in todo and there is no codex id associated with it when a use presses c they shuold jump into a new codex session that gets crated for that task and appended like the other tasks. this allows the user to plan and chat before creating the single tssk or if they have a task created they can chat about it and maybe update the task.

Completion note:
COMPLETED 2026-09-20: Added c on unlinked Todo tasks to create a durable Codex planning conversation with full task context, retain Todo status, and reopen the same linked session. Reused the interactive guardian and project lease; protected concurrent task edits and allowed subsequent Git-off task activation. Updated help, README, and changelog. Checks: cargo test --locked --all-targets --all-features (654 passed; one optional installed-Codex test run separately and passed); cargo clippy --no-deps --locked --all-targets --all-features -- -D warnings (passed); rustfmt --edition 2024 --check --config skip_children=true on all changed Rust files (passed); cargo test --locked --bin clt installed_codex_planning_session_is_durable -- --ignored --nocapture (passed); cargo rustc --locked --bin clt -- -C debuginfo=1 and python3 /tmp/clt-planning-tui-smoke.py against a copied source-current binary (passed: one creation, two guarded resumes of the same ID, Todo unchanged). Repository-wide rustfmt check reproduces exactly the same four existing diffs on frozen revision 5f9e95d2e6b2bcc463dce568baa64d5ec2081b2b via isolated git archive. Queued linked Todo follow-up tasks/todo/0004-format-the-existing-orphan-journal-code-so-the-r.md with baseline evidence; no follow-up work started.

codex:01a0bf48-9ca0-7cc1-9401-ad35975cc5bb
