Document Linux Codex Bubblewrap and AppArmor setup (DOCS, LINUX, AGENT)

Completion note: COMPLETED 2026-07-15: Added Linux Codex sandbox prerequisites to the README, including Bubblewrap installation, the Ubuntu 24.04 AppArmor profile fix for `RTM_NEWADDR` failures, sandbox verification, and a warning against disabling the global AppArmor restriction. Verified with `git diff --check`.
