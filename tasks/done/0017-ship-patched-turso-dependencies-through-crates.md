Ship patched Turso dependencies through crates.io and verify packaged builds (BUG, HIGH)

Release preparation note:
2026-09-16: Revised the release to bundle the patched Turso engine, SDK kit, and Rust API directly inside the single clt-rs 0.6.15 package, as requested. No separate fork publication is needed. Removed the unused sync packages, standalone vendor tools, and nested Cargo manifests. Retained upstream licenses/provenance, the four WAL fixes, and the compile-time patch marker. Updated the release artifact audit and Linux/macOS CI for a single normalized archive, and simplified publication to one cargo publish command.

VERIFIED 2026-09-16 on macOS with stable Rust 1.97.1: All 622 tests passed (47 bundled engine tests, 557 CLT unit tests including registry recovery regressions, 4 architecture checks, and 14 CLI tests). Application/build-script formatting, strict CLT Clippy, and diff checks passed. The single 2.8 MiB archive built from its normalized published manifest and passed source/provenance/dependency audits plus version, task, and registry persistence smoke checks. Negative checks rejected a missing patched WAL source file and a separate upstream core dependency. Extracted-archive installations passed both with --locked and with its lockfile removed (debug profile, offline registry cache), reporting clt 0.6.15. These are local artifact installation checks, not public crates.io installs. All 244 retained core Rust files match the prior patched engine after reversing the mechanical bundling cfg changes and library module wiring. Only three vendor source directories remain; no nested manifests or unused sync packages remain. Cargo emits a documented deprecation warning for the engine library target retaining Rust 2021.

Publication pending: Commit/push and public publication remain unperformed. Linux CI and clean public installations with and without --locked still need verification during rollout. Keep this task in Doing until the release reaches users.
