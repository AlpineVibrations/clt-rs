Fix idle enabled workers not auto-recovering and expose the reason in the TUI (BUG, HIGH) — COMPLETED 2026-09-02: Restored automatic prelaunch task-board checkpointing for Git-enabled agents without absorbing unrelated dirty work; surfaced failed pending projects as TUI errors with retry timing and an immediate retry action; updated bundled workflow guidance and bumped CLT to 0.5.4. Checks: `cargo test --locked -- --test-threads=1` (395 passed), `cargo clippy --locked --all-targets -- -D warnings`, `cargo fmt --all -- --check`, and `git diff --check`.
