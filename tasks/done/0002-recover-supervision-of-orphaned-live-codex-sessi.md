Recover supervision of orphaned live Codex sessions without duplicating the session (BUG, AGENT, SESSION-CONTROL, FISHDOME)

Completion note:
COMPLETED 2026-09-09: Added automatic replacement supervision for surviving automated Codex process groups, retaining the exact project/run/session identity and live output. macOS audit-token and Linux pidfd controls prevent signaling reused process IDs; verified group anchors cover leaderless groups and tool descendants. Atomic claims, exact finalization, and lease-expiry guards prevent competing controllers. Stop and interactive takeover work again, and c uses the same guarded takeover as i for active sessions. Added recovery without an active daemon when the old process already exited. Verification on macOS: cargo fmt --all -- --check; cargo clippy --locked --all-targets --all-features -- -D warnings; cargo test --locked --all-targets --all-features -- --test-threads=4 (520 unit, 4 architecture, 14 CLI tests passed); cargo build --release --locked; git diff --check. The initial unconstrained parallel test run hit an existing registry-access lock race; its isolated rerun and the full four-thread run passed. Installed the verified build and restarted launchd (first bootstrap attempt hit a transient removal race; retry succeeded). Installed, release, and service-snapshot SHA-256 values match. Service active with current schema and zero active workers/leases; Fishdome completed FISH-34 independently and remains paused with an empty Doing list. Linux implementation was reviewed but not executed on this macOS host.
