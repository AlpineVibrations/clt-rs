Recover session-linked Doing tasks after timeout and back off contradictory no-task runs (BUG, AGENT, FISHDOME)

Completion note:
COMPLETED 2026-09-08: Recovered interrupted Doing selection from its saved Codex session after normal timeout cleanup removes the worker and lease. Exact recovery preserves stopped sessions and unrelated Doing tasks and honors failure backoff. Contradictory idle reports now record a failure when ready Todo work, a linked active task, or a board scan error remains. Updated recovery prompt and documentation. Checks: cargo fmt --all -- --check; cargo clippy --locked --all-targets --all-features -- -D warnings; cargo test --locked --all-targets --all-features (508 passed); cargo build --release --locked. Installed the verified binary and restarted the scheduler; installed and service snapshot hashes match, service is active with current schema, zero workers/leases, and FISHDOME remains paused with seven Todo tasks and one Doing task. The original FISH-26 attempt timed out after 2700 seconds and was followed by 132 no-task runs; existing project files and session markers were preserved.
