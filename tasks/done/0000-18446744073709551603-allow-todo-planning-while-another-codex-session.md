Allow Todo planning while another Codex session is queued for recovery. Reproduce the cannatrace-solid resume_requested state, preserve its recovery control and Git journal, and continue rejecting live or claimed sessions. (BUG)

COMPLETED 2026-09-21: Planning now accepts unclaimed queued recovery sessions behind its exclusive project lease. Atomic registration and interactive reservation still reject live sessions, unfinished workers, and lost/expired leases. Added regression coverage reproducing the reported error, preserving the recovery control and WORKING Git journal through planning exit/reopen, and rejecting ownership races. Checks: `rustfmt --edition 2024 --check build.rs src/main.rs src/lib.rs tests/architecture.rs tests/cli.rs`; `cargo clippy --no-deps --locked --all-targets --all-features -- -D warnings`; `cargo test --locked --all-targets --all-features` (663 passed, 1 optional test ignored); `cargo rustc --release --locked --bin clt -- -C debuginfo=1`; PTY smoke with real Codex app-server and mock interactive resume created one durable conversation, reopened the same ID twice, and preserved Todo. Installed the verified release binary locally and verified byte equality. The cannatrace-solid recovery records were inspected read-only and left intact.

COMPLETED 2026-09-21: Prepared patch release 0.6.20 in Cargo.toml, Cargo.lock, and the dated changelog. `CARGO_NET_OFFLINE=true python3 scripts/check_release.py --allow-dirty` passed archive build/source verification and packaged version, task, and registry persistence smoke checks. Included the fix and release metadata in the user-requested commit.
