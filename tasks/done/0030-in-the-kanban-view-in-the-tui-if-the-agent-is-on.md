in the kanban view in the tui if the agent is on then it should show agent status in the console title after the project name console text that is there already. if agent is not on or not registered just omit the agent text from the console title

Completion note:
COMPLETED 2026-09-10: Show the active enabled registered project's cached agent status after its Kanban Console title; omit it for off/unregistered projects, preserve backlog and other view titles, and document the behavior. Checks: cargo fmt --all -- --check; cargo clippy --locked --all-targets --all-features -- -D warnings; git diff --check. cargo test --locked --all-targets --all-features: 521/523 library tests passed, including all TUI tests; the two Git identity fixture failures reproduced on frozen dc9c8df062a946d75a7eda806cd7c4cfd9c01bde in an isolated git-archive export. With GIT_AUTHOR_NAME, GIT_AUTHOR_EMAIL, GIT_COMMITTER_NAME, and GIT_COMMITTER_EMAIL unset only for test subprocesses, the full rerun passed 522/523; its transient registry-lock failure passed on exact-test retry, and all 523 baseline library tests passed. cargo test --locked --all-features --test cli with the same test-only environment isolation passed all 14 tests. Queued linked Todo tasks/todo/0002-isolate-human-commit-test-fixtures-from-inherite.md for the reproducible identity failures, with exact baseline commands and evidence; no follow-up work started.

codex:01a08c08-7ae0-79d0-bc01-ccec2c901fb3
