Allow automated Git tasks to start with pre-existing staged changes while preserving user work (BUG)

Completion note:
COMPLETED 2026-09-16: Replaced the clean-index startup refusal with a frozen index-tree baseline, preserved existing staged entries (including partially staged board files) across task-board checkpoints, skipped startup pulls in dirty checkouts, and documented task-only commits using a separate index when needed. Added regressions for staged additions, deletions, renames, binary content, partial staging, legacy journals, index drift, and complete finalization without consuming unrelated staging. Checks: 88 managed-Git tests; rustfmt; cargo clippy --no-deps --locked --all-targets --all-features -- -D warnings; cargo test --locked --all-targets --all-features -- --test-threads=4 (626 passed). An initial full-suite database fixture lease failure passed independently and in the complete rerun. Built and installed the release binary, restarted launchd successfully after one transient bootstrap failure, verified the scheduler binary hash, and confirmed Terraqua running without a worker error.
